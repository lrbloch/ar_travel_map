using UnityEngine;
using System.Collections;

public class SetLocation : MonoBehaviour
{
    public string imageFolder;
    public string URL;

    // Use this for initialization
    void Start()
    {
        //LoadImage.SetImageFolder(imageFolder);
        //OpenUrl.SetUrl(URL);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
